/**
 * @format
 */
import React, { PropsWithChildren } from 'react';

function Auth({ children }: PropsWithChildren<any>) {
  React.useEffect(() => {
  }, []);

  return children;
}

export default Auth;
